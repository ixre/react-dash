import { LOGING } from "./Action";
export default function user (state = {}, action) {
    switch (action.type) {
        case LOGING: {
            break;

        }
        default:
    }
    return state;
}
