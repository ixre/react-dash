import { LOGING } from "./Action";
const initialState = {
    user: { }
};
export default function login(state = initialState, action) {
    switch (action.type) {
        case LOGING: {
            break;

        }
        default:
    }
}
